# Copy this file to both .env.local (monorepo root, for packages/db migrate)
# AND apps/web/.env.local (for Next.js — it does not read the monorepo root).
# Both files are gitignored.

# ============================================================
# Supabase
# ============================================================
# Get these from: https://supabase.com/dashboard → project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# ============================================================
# Prisma / Postgres
# ============================================================
# DATABASE_URL: pgBouncer pooled connection (port 6543) — used at runtime
# DIRECT_URL:   direct connection (port 5432) — required by `prisma migrate`
# Both strings available in: Supabase → Settings → Database → Connection string
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# ============================================================
# Redis (Upstash)
# ============================================================
# Use the rediss:// connection string from Upstash console — NOT the REST API URL.
# BullMQ requires a real Redis connection via ioredis; the @upstash/redis REST SDK
# is incompatible with BullMQ.
REDIS_URL=rediss://default:<token>@<hostname>.upstash.io:6380

# ============================================================
# Anthropic
# ============================================================
ANTHROPIC_API_KEY=sk-ant-...

# ============================================================
# Adzuna (free key at developer.adzuna.com)
# ============================================================
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
